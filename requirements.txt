# 实验组 AI 大模型 API 中转站 — Phase 1 MVP 依赖
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
SQLAlchemy>=2.0,<3.0
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
PyJWT>=2.8
cryptography>=42.0
httpx>=0.27
python-multipart>=0.0.9

# 生产环境可选（按需安装）：
# psycopg[binary]>=3.1     # PostgreSQL 驱动
# redis>=5.0               # 分布式限流/缓存（默认使用进程内限流）
